name: mystore
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.1.0
  device_apps: ^2.2.0
  permission_handler: ^12.0.1
  firebase_core: ^4.2.1
  firebase_storage: ^13.0.4
  file_picker: ^10.3.7  
  uuid: ^4.2.1

  # حزم لخدمة التحديث
  package_info_plus: ^9.0.0   # لمعرفة الإصدار المثبت حالياً
  dio: ^5.4.0                 # لتحميل ملف الـ APK
  path_provider: ^2.1.1       # لمعرفة مسار حفظ الملف
  open_filex: ^4.3.4          # لفتح ملف الـ APK (لبدء التثبيت)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0


flutter:


  uses-material-design: true
